[package]
name = "typikon-core"
description = "Deterministic evaluation engine for Typikon definition packs"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
conformance = ["typikon-loader/filesystem"]

[dependencies]
chrono.workspace = true
serde_json.workspace = true
thiserror.workspace = true
typikon-loader = { path = "../typikon-loader", default-features = false }
typikon-schema = { path = "../typikon-schema" }

[dev-dependencies]
typikon-loader = { path = "../typikon-loader", default-features = false }

[lints]
workspace = true
